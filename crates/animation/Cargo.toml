[package]
name = "animation"
version = "0.2.0"
edition = "2021"

[dependencies]
bevy = "0.16"
bevy_ggrs = "0.18.0"

serde = { version = "1.0", features = ["derive"] }

bevy_common_assets = { version = "0.13", features = ["ron"] }